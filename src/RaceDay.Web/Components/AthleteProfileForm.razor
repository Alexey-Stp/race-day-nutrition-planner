@using RaceDay.Core

<div class="form-card">
    <h2>👤 Athlete Profile</h2>
    <div class="form-group">
        <label for="weight">Body Weight</label>
        <div class="input-group">
            <input type="number" id="weight" @bind="AthleteWeight" class="form-control" 
                   min="40" max="150" step="0.5" placeholder="e.g. 75" />
            <span class="input-unit">kg</span>
        </div>
        <small class="form-text">Athlete's body weight affects fluid and sodium recommendations.</small>
    </div>
</div>

@code {
    [Parameter]
    public double AthleteWeight { get; set; } = 75;

    [Parameter]
    public EventCallback<double> AthleteWeightChanged { get; set; }
}
